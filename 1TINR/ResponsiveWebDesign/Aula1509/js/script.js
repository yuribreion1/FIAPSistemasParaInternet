function btnMenu() {
    var menu = document.getElementsByClassName('menu');
    // Quebrando a chamada direta do menu
    menu[0].style.display = "block";
}

function btnClose() {
    var menu = document.getElementsByClassName('menu');
    // Quebrando a chamada direta do menu
    menu[0].style.display = "none";
}